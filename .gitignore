# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories (uncomment to ignore if you're not using Go modules or if you prefer to manage vendor separately)
vendor/
build/

# Project-specific binaries (replace 'myprogram' with your actual program name)
# This ignores the executable generated by `go build` in the root directory.
# If your main package is in a subdirectory, you might need to adjust this.
/myprogram 

# IDE and OS specific files
.vscode/
.idea/
.DS_Store
Thumbs.db
